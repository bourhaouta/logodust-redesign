//- style
mixin style(source, unminified)
	if unminified
		link(href=dist.css + source + '.css?v=' + site.version, rel='stylesheet')
	else
		link(href=dist.css + source + '.min.css?v=' + site.version, rel='stylesheet')